# Deploy to GitHub Pages

Run this in Terminal whenever you make changes:

```bash
cd "/Users/macbook/Desktop/website portfolio"
git add .
git commit -m "update"
git push
```

Site is live at: https://leontelemaquedisplays.github.io

Updates go live within ~1 minute of pushing.
